apiVersion: v1
kind: Service
metadata:
  name: pingpong-svc
spec:
  type: ClusterIP
  selector:
    app: pingpong
  ports: # will let TCP traffic from port 1234 to port 3000
    - port: 1234
      protocol: TCP
      targetPort: 3000
