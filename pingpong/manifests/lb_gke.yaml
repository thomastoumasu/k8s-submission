apiVersion: v1
kind: Service
metadata:
  name: lb-svc
  namespace: exercises
spec:
  type: LoadBalancer
  selector:
    app: pingpong
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3002 #check env in deployment
